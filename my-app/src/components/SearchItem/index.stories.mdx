import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import SearchItem from '.';

<Meta title="Components/SearchItem" component={SearchItem} />

# Search SearchItem

With `MDX` we can define a story for `SearchItem` right in the middle of our
Markdown documentation.

<!--- This is your Story template function, shown here in React -->

export const Template = (args) => <SearchItem {...args} />

<Canvas>
  <Story name="Default" args={{
      label: 'Default story',
      id: 'https://id.com/1'
    }}>
    {Template.bind({})}
  </Story>
</Canvas>
